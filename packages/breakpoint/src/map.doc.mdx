# map(values, mapValueToStyle)

## Import
```
import { map } from '@lendi-ui/breakpoint';
```

## Usage

`map` will match different breakpoint style for this property.
```
<ComponentName size={{ mobile: 'xs', tablet: 'sm', 'desktop': 'lg'}} />
```