## Usage

Depth that style box-shadow for HTML element. Depth receive integer paremeters from [ 1, 2, 3, 4].
```
import { depth } from '@lendi-ui/depth';
```

```js
const Panel = styled.div`
  ${depth(4)}
`;

render(<Panel>Such depth!</Panel>)
```
