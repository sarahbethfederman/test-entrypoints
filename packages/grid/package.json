{
  "name": "@lendi-ui/grid",
  "version": "2.1.0",
  "description": "Grid utilities for layout.",
  "source": "src/index.ts",
  "main": "dist/cjs/index.js",
  "types": "dist/types/index.d.ts",
  "files": [
    "dist"
  ],
  "license": "ISC",
  "dependencies": {
    "@lendi-ui/breakpoint": "^2.0.0"
  },
  "peerDependencies": {
    "styled-components": "^3"
  },
  "scripts": {
    "build:cjs": "tsc --project tsconfig.cjs.json",
    "build:types": "tsc --project tsconfig.types.json",
    "build": "yarn run build:types && yarn run build:cjs"
  }
}
