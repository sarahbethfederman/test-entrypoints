import {PropTable} from '@lendi-ui/doc-utils';

## Usage

```
import { Fade } from '@lendi-ui/transition';
```

```jsx
<Fade active={true} timeout={340}>
  <div>This will be the child component</div>
</Fade>
```

## Fade Properties

<PropTable>
  <PropTable.Entry name="active" type={ 'boolean' } defaultValue="false" description=""/>
  <PropTable.Entry name="timeout" type={ 'number' } defaultValue={250} description="Time to animate fading transition (in milliseconds)"/>
</PropTable>
