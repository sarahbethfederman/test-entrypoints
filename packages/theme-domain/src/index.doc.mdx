## About
This repo represents a collection of colors, fonts, typography, etc,
as part of Domain theme

```jsx
import Theme from '@lendi-ui/theme';
import { theme as domainTheme } from '@lendi-ui/theme-domain'

  <div> 
    <Theme kind={domainTheme}>
      ...New stuff using LUI components
    <Theme/>
  </div>
```