import {PropTable} from '@lendi-ui/doc-utils';

## Usage

```
import Popover from '@lendi-ui/popover';
```

```jsx
<>
  <div>
    <Popover
      content={<><Body size='sm' mb='xxs'>Popover Heading</Body><Body size='xs'>This is LUI popover on content body. You can put extra infomation in here.</Body></>}
      position='top'
      target={<Body variant="primary" size="sm" style={{display: 'inline'}}><QuestionOutline color="shade.1000" /> You can trigger a popover on any React Component</Body>}
    />
  </div>
  <Popover
    content={<Body size='sm' mb='xxs'>I pop out from the left</Body>}
    position='left'
    target={<ChevronLeft color="secondary.600" />}
  />
</>
             
```
### Design Notes
Popovers are meant to give related additional content. They can be much more verbose (compared to Tooltips) and they can include a header and many lines of text in the body.

## Properties

<PropTable>
  <PropTable.Entry name="content" type="React.ReactNode" description="Popover content"/>
  <PropTable.Entry name="className" defaultValue=""type="string" description="Custom class name"/>
  <PropTable.Entry name="position" type="{'top' | 'bottom' | 'left' | 'right'}" defaultValue="top" description="Popover content display position comparing with target component"/>
  <PropTable.Entry name="target" type="React.ReactNode" description="Target component that trigger Popover by clicking it"/>
</PropTable>

### Notes

<b>Support for native props like Aria attributes, Standard HTML Attributes like title, classname, id, role, itemProp, itemID, itemRef</b>

