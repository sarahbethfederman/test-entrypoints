import {PropTable} from '@lendi-ui/doc-utils';

## Usage

```
import {Radio} from '@lendi-ui/radio';
```

```jsx

<Radio value='1' label='LUI single Radio Button' onChange={() => {}} />

```

## Properties
<PropTable>
  <PropTable.Entry name="isBoxed" type="boolean" defaultValue={false} description="Whether the Radio is boxed or not."/>
  <PropTable.Entry name="isChecked" type="boolean" defaultValue={false} description="Whether the Radio is checked or not."/>
  <PropTable.Entry name="isDisabled" type="boolean" defaultValue={false} description="Whether the Radio is disabled or not."/>
  <PropTable.Entry name="label" type="string" defaultValue="" description="Radio display value."/>
  <PropTable.Entry name="onChange" type="(event: React.ChangeEvent<HTMLInputElement>) => void;" description=""/>
  <PropTable.Entry name="size" type="'xs' | 'sm' | 'md' | 'lg'"  defaultValue="md" description="Size of the radio"/>
  <PropTable.Entry name="value" type="string" description="Radio value."/>
</PropTable>


### Notes
Radio should only be used when there are two or more options. If there is a single option, consider using a checkbox instead. 

```



